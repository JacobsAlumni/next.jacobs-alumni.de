import * as React from "react";
import Layout from "../layout/index";

export default class Home extends React.Component {
    render() {
        return <Layout>
            This is the new jacobs-alumni.de website, rewritten in NextJS. ]
            Still working on it, but I'm testing the layout. 
        </Layout>
    }
}